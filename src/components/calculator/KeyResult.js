import React from 'react'

const KeyResult=(props)=>{
  const {
    result
    
  }=props

  return(
    <div className="screen-row">

    <p>{result}</p>

    </div>
  )
}

export default KeyResult
  // <input {...result}/>
